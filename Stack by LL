#include<stdio.h> 
#include<stdlib.h> 
struct stack 
{ 
int number; 
struct stack *next; 
}*p,*q,*top; 
void init(); 
void push(int); 
int full(); 
int pop(); 
int empty(); 
void display(); 
int main() 
{ 
int ch, num; 
init(); 
 do 
 { 
  printf("\n Menu \n 1. Push \n 2. Pop \n"); 
  printf("\n Enter your choice : "); 
  scanf("%d", &ch); 
  switch(ch) 
  { 
   case 1 :  
     printf("\n Enter the data : "); 
     scanf("%d", &num); 
     push(num); 
     display(); 
   break; 
      
   case 2 : 
     num = pop(); 
     if(num != -9999) 
     { 
      printf("\n Element popped = %d", num); 
      display(); 
     } 
   break; 
  } 
 }while(ch<3); 
} 
 
void init() 
{ 
top = NULL; 
} 
void push(int num) 
{ 
} 
p = (struct stack *)malloc(sizeof(struct stack)); 
p->number = num; 
p->next = NULL; 
if(top == NULL) 
{ 
} 
else 
{ 
} 
int empty() 
{ 
top = p; 
p->next = top; 
top = p; 
if(top == NULL) 
return 1; 
else  
} 
return 0; 
 
void display() 
{ 
 if(empty() == 1) 
 { 
  printf("\n Stack is EMPTY !\n"); 
 } 
 else 
 { 
  p = top; 
  while(p != NULL) 
  { 
   printf("\n\t%d", p->number); 
   p = p->next; 
  } 
 } 
} 
 
int pop() 
{ 
 int num; 
 if(empty() == 1) 
 { 
  printf("\n Stack is EMPTY !\n"); 
  return -9999; 
 } 
 else 
 { 
  p = top; 
  num = top->number; 
  top = top->next; 
  free(p); 
  return num; 
 } 
} 
